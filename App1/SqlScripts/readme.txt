Syntax:
{file sequence NO}.{table name}_[data/schema].sql

Note:
Each table can only contain no more than two script files, 
which are xxx_schema.sql and xxx_data.sql


Samples:
01.resources_schema.sql
02.resources_data.sql
